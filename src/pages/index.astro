---
// This page instantly redirects based on browser language.
// Using client-side JS for fastest redirect on static hosting.
---

<html>
  <head>
    <meta charset="utf-8" />
    <script is:inline>
      const lang = navigator.language || navigator.userLanguage || '';
      const path = lang.toLowerCase().startsWith('ja') ? '/ja/' : '/en/';
      location.replace(path);
    </script>
  </head>
  <body></body>
</html>
